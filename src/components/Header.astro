---
// components/Header.astro
const { imageSrc, title } = Astro.props;
---

<header class="relative h-[20vh] w-[80vw] mx-auto rounded-lg shadow-md">
  <img src={imageSrc} alt="Header Image" class="absolute inset-0 w-full h-full object-cover rounded-lg" />
  <div class="absolute inset-0 bg-black/50 flex items-center justify-center rounded-lg">
    <h1 class="text-blue-300 text-3xl font-bold text-center">{title}</h1>
  </div>
</header>
